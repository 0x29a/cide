import json

import requests
from flask import Flask
app = Flask(__name__)

@app.route('/')
def healthcheck():
    response = requests.get('http://127.0.0.1:8137/healthz').json()
    return json.dumps(response)
